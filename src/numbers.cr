puts 1 <=> 1, # 0
  2 <=> 1,    # 1
  1 <=> 2     # -1

puts -5.abs, # absoulte value 5, without -
  4.3.round, # 4.0
  5.even?,   # false, is an odd number
  10.gcd(16) # maximo divisor comum

puts Math.cos(1),  # cosseno em radianos
  Math.sin(1),     # seno em radianos
  Math.tan(1),     # tangente em radianos
  Math.log(42),    # logaritmo natural
  Math.log10(312), # logaritmo na base 10
  Math.sqrt(9)     # raiz quadrada, 3

  # Math::E, # Euler's number
  # Math::TAU,  # Full circle constant (2 * PI)
  # Math::PI    # Archimedes' constant (TAU / 2)
